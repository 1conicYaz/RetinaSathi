# InsForge Configuration
# Get these values from your InsForge dashboard: https://insforge.dev

# Your InsForge backend URL (required for authentication)
VITE_INSFORGE_BASE_URL=https://your-app.region.insforge.app

# Anonymous key (required for auth, database, and storage operations)
VITE_INSFORGE_ANON_KEY=your_insforge_anon_key_here

# Authenticated InsForge function that securely proxies cloud inference.
VITE_INFERENCE_FUNCTION=retinasathi-inference

# auto tries the local desktop service first, then the cloud service.
# Use local or cloud to force a single runtime.
VITE_INFERENCE_MODE=auto
VITE_LOCAL_INFERENCE_URL=http://127.0.0.1:8000
